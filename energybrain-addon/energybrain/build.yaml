build_from:
  aarch64: ghcr.io/home-assistant/aarch64-base-python:3.12
  amd64: ghcr.io/home-assistant/amd64-base-python:3.12

# The Dockerfile copies from the repo root (pyproject.toml + energybrain/).
# When building locally: docker build -f energybrain-addon/energybrain/Dockerfile .
# HA add-on builder uses this file to set the build context.
args:
  BUILD_FROM: ""
